# DISPARITY-{NNN}: {Brief Title}

> **DO NOT EDIT THIS FILE** - This is a template. Copy to `DISPARITY-{NNN}.md`

**Category:** Protocol | Sync | Auth | Connection | Config  
**Priority:** P0 (Blocking) | P1 (High) | P2 (Medium) | P3 (Low)  
**Estimate:** {hours}h

---

## Files Affected

| Role | File Path |
|------|-----------|
| .NET (to fix) | `server/csharp/src/SyncKit.Server/{path/to/file.cs}` |
| TypeScript (reference) | `server/typescript/src/{path/to/file.ts}` |

---

## Current Behavior (.NET)

```csharp
// Paste relevant .NET code snippet here
```

**Issue:** {Describe what's wrong}

---

## Expected Behavior (TypeScript Reference)

```typescript
// Paste relevant TypeScript code snippet here
```

**Correct behavior:** {Describe what it should do}

---

## Disparity Details

| Aspect | TypeScript | .NET (Current) | Required Change |
|--------|------------|----------------|-----------------|
| {property name} | `{TS type}` | `{C# type}` | Change to `{new C# type}` |
| {JSON property} | `"{jsonName}"` | `"{currentName}"` | Add `[JsonPropertyName("{jsonName}")]` |
| {default value} | `{TS default}` | `{C# default}` | Change default to `{new default}` |

---

## Suggested Fix

1. {Step 1 - specific instruction}
2. {Step 2 - specific instruction}
3. {Step 3 - specific instruction}

**Code change example:**
```csharp
// Before
public DateTime Timestamp { get; set; }

// After
[JsonPropertyName("timestamp")]
public long Timestamp { get; set; }
```

---

## Acceptance Criteria

- [ ] Property type matches TypeScript reference
- [ ] JSON serialization produces identical output
- [ ] Code compiles without errors
- [ ] Existing unit tests pass
- [ ] (If applicable) Integration tests pass

---

## Related

- **Audit Document:** [AUDIT-TYPESCRIPT-PARITY.md](../AUDIT-TYPESCRIPT-PARITY.md)
- **Related Disparities:** DISPARITY-{XXX}, DISPARITY-{YYY}
- **Phase Work Item:** (if applicable)

---

**Status:** ⬜ Not Started
