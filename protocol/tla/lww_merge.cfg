SPECIFICATION Spec

\* Smaller configuration for faster verification
CONSTANTS
    Clients = {c1, c2}
    MaxTimestamp = 2
    Fields = {f1}

\* Check the critical behavioral properties
INVARIANTS
    Convergence
    Determinism

\* Allow system to reach stable states
\* Convergence: All replicas reach same field values
\* Determinism: Same inputs produce same outputs
