\* TLC Model Checking Configuration for fugue_determinism.tla (SIMPLIFIED)
\*
\* Simplified to verify basic determinism properties that hold with out-of-order delivery

SPECIFICATION Spec

CONSTANTS
  Clients = {c1, c2, c3}
  MaxClock = 10
  NULL = NULL

\* =============================================================================
\* INVARIANTS (Simplified - only properties that hold with arbitrary delivery)
\* =============================================================================

\* NodeId ordering properties (FUNDAMENTAL - these should hold)
INVARIANT NodeIdTotalOrder
INVARIANT NodeIdAntisymmetric
INVARIANT NodeIdTransitive
INVARIANT NodeIdIrreflexive
INVARIANT NodeIdOrderingCorrectness

\* Base invariants
INVARIANT TypeInvariant
INVARIANT ReplicaStateInvariant

\* Disabled - too strict or depend on temporal formulas:
\* INVARIANT BTreeMapAlwaysSorted
\* INVARIANT SameBlocksSameOrder
\* INVARIANT SameOperationsSameState
\* INVARIANT DeterministicConflictResolution

\* =============================================================================
\* PROPERTIES (Disabled - most use temporal formulas)
\* =============================================================================

\* PROPERTY TiebreakerConsistency
\* PROPERTY FindOriginsDeterministic
\* PROPERTY DeleteRangeDeterministic
\* PROPERTY MergeDeterministic
\* PROPERTY LamportClockNeverDecreases
\* PROPERTY ClockUpdateCorrectness

\* =============================================================================
\* STATE CONSTRAINTS
\* =============================================================================

CONSTRAINT StateConstraint

\* =============================================================================
\* EXPECTED RESULTS
\* =============================================================================

\* Reduced state space for faster verification
\* Verifies basic NodeId ordering and type safety
