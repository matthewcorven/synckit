SPECIFICATION Spec

\* Define the model parameters (smaller for faster verification)
CONSTANTS
    Clients = {c1, c2}
    MaxOperations = 3
    Fields = {f1}

\* Check THE MOST IMPORTANT STATE PROPERTIES
INVARIANTS
    StrongEventualConsistency
    OrderIndependence

\* This proves Strong Eventual Consistency - the key property!
\* Note: NoDataLoss and other temporal properties require PROPERTIES section
