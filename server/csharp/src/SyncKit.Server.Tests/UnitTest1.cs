namespace SyncKit.Server.Tests;

/// <summary>
/// Placeholder test to verify test project setup.
/// </summary>
public class ProjectSetupTests
{
    [Fact]
    public void ProjectSetup_IsValid()
    {
        // This test verifies the test project is correctly configured
        // and can reference the main SyncKit.Server project.
        Assert.True(true);
    }
}
